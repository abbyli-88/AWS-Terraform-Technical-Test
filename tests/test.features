Feature: EC2 related general feature

    Scenario: Ensure the amount of EC2
        Given I have aws_instance defined
        When I count them
        Then I expect the result is 3

	Scenario: Ensure type of EC2 instance
		Given I have aws_instance defined
		Then its instance_type must be t3.micro
